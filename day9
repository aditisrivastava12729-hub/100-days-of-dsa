# Recursive function to reverse string
def mirror_string(s):
    # Base case
    if len(s) == 0:
        return s
    # Recursive case
    return mirror_string(s[1:]) + s[0]

# Taking input
s = input().strip()

# Printing result
print(mirror_string(s))
